{
  "name": "betinc-aggregator-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BetInc aggregator gateway service foundation.",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc --project tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "express": "^4.18.3",
    "pino": "^9.0.0",
    "pino-http": "^10.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "tsx": "^4.7.1",
    "typescript": "^5.4.5",
    "vitest": "^1.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
